# Changelog

本项目遵循语义化版本。预发布阶段的接口和数据结构仍可能变化。

## 0.1.0-alpha.3 - 2026-08-12

- 将工作区绑定到具体会话：新建会话必须先选择工作区，恢复旧会话始终使用其原工作区，不再隐式继承上一次路径。
- 修复长会话把输入框推出窗口、页面无法滚动的问题，并让刷新按钮同步重新获取状态、历史记录和模型列表。
- 修复运行中按钮看似无响应的问题；会话切换、新建和终端交接只在实际过渡期间禁用，并提供可见反馈。
- 原始 OMP 终端现在以可见 Windows Terminal 窗口打开，并通过持久交接锁避免桌面端与终端同时写入同一会话。
- 将“移到回收站”和“彻底删除”拆分为两个明确操作；永久删除需要输入确认文字，并安全处理关联附件。
- 强化跨 WSL 安装的会话索引、异步加载和元数据隔离，避免相同 Linux 路径误操作其他发行版的数据。
- README 明确标注项目仍处于主动调试的实验性 Alpha；当前版本仅支持 Windows 桌面端连接 WSL2 OMP，原生 Windows OMP 列为后续适配目标。

## 0.1.0-alpha.2 - 2026-08-12

- 修复旧会话恢复后的发送链路，并显示 OMP 异步命令错误、恢复失败草稿。
- 串行化单运行时切换，精确回收 WSL OMP 进程组，避免旧会话孤儿进程。
- 修复 Windows Terminal 启动参数并把启动失败反馈到界面。
- 终端交接与回收操作现在会先验证运行时已停止，并在交接成功后切换到新会话，避免并发写入同一 JSONL。
- 适配 OMP `isTerminal` 与 `prompt_result` 生命周期帧，支持异步 continuation 和本地命令完成。
- 增加 OMP 原生可用模型列表和模型切换。
- 将侧边栏会话操作升级为菜单，增加确认后的可恢复回收站。
- 新增英文默认 README、中文 README 和无真实模型调用的恢复/所有权端到端测试，并纳入 CI。

## 0.1.0-alpha.1 - 2026-08-12

- 首个公开 Alpha。
- Windows + WSL2 OMP 自动发现与工作区启动。
- OMP RPC v1/v2 解码、流式会话、工具卡和扩展确认 UI。
- 现有 OMP 会话索引、搜索、置顶、归档和恢复。
- OMP 内置与自定义主题同步，包含浅色、深色和色盲模式。
- 原始终端回退、设置诊断和本地 SQLite 界面元数据。
